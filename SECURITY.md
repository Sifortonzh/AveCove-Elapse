# 安全说明

- 原始学号不会写入数据库，而是通过服务器密钥生成不可逆同步标识。
- 会话 Cookie 使用 HttpOnly、SameSite=Lax，生产环境仅通过 HTTPS 发送。
- 邮箱登录采用 10 分钟验证码；生产环境必须配置 SMTP。
- 评论写入有频率限制、敏感词预审、举报、隐藏、禁言和管理员操作记录。
- AI 请求有每日额度，密钥只保存在服务器环境变量中。
- PostgreSQL 不开放公网端口；Caddy 统一处理 HTTPS 与安全响应头。
- 发现安全问题时，请勿在公开 Issue 中附带真实学号、邮箱、密钥或题库文件。
